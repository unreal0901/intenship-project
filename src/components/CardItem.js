import React from "react";

const CardItem = ({ id, name, type, link }) => {
  return (
    // <div className="card" key={id}>
    //   <p className="name">{name}</p>
    //   <p className="type">{type}</p>
    //   <p className="link">{link}</p>
    // </div>
    <div></div>
  );
};

export default CardItem;
